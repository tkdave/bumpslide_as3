# IMPORTANT Change to your local system paths before using ANT

#Toggle on/off specific targets
run.test = true
run.asdoc = true
autolaunch.url = true

# Configure Flex SDK Location
FLEX_HOME=/Users/tkdave/resources/flash/lib/flex4_sdk


project.name=Bumpslide AS3 Library
project.filename=bumpslide_as3_lib

#browser
browser = Safari
report.url = ${report.loc}/html/index.html


## Don't change these settings ###
 
#paths

doc.src.loc=${basedir}/src/com/bumpslide
swc.src.loc=${basedir}/src/com/bumpslide
main.src.loc=${basedir}/src
test.src.loc=${basedir}/test
doc.loc=${basedir}/doc
lib.loc=${basedir}/build/libs
bin.loc=${basedir}/bin
report.loc=${basedir}/report
template.loc=${basedir}/build/templates